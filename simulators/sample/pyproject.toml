[project]
name = "sample"
version = "0.1.0"
description = "Flow simulator for the analog flow meter app"
requires-python = ">=3.11"
dependencies = [
    "pydoover>=1.5.0",
]

[project.scripts]
doover-app-run = "main:main"
